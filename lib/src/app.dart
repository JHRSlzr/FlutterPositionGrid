import 'package:flutter/material.dart';
import 'package:flutter_controles/src/pages/controles.dart';

class MyApp extends StatelessWidget {
  Widget build(context) {
    return MaterialApp(
        home: Center(
      child: ControlesPage(),
    ));
  }
}
